# Validation Glossary — folio-lab

ทะเบียนกลางของข้อความ validation/error ที่ผู้ใช้เห็น — **การ์ด story ต้องอ้างรหัสจากที่นี่ ห้ามคิดข้อความใหม่ในการ์ด**

## กติกา

1. **หนึ่งความหมาย = หนึ่งรหัส** — ถ้าความหมายตรงกับรหัสที่มีอยู่ ให้ใช้ซ้ำ อย่าสร้างรหัสใหม่
2. ข้อความเป็น**ภาษาไทย** เขียนแบบบอกวิธีแก้ ไม่ใช่บอกว่าผู้ใช้ผิด และต้องผ่านกฎ copy (R12):
   ห้ามศัพท์ implementation (`API`, `provider`, `cache`, `endpoint`, `stub`, `fetch`, …), ห้าม `—`
3. เพิ่ม/แก้กฎ gate เมื่อไหร่ ต้องขยับพร้อมกันสี่ที่ (R7): **BR** ในการ์ด · **AC** แถวที่ตรงกัน ·
   **ตาราง Validation** ของการ์ด · ไฟล์นี้
4. รหัสที่เลิกใช้ **ห้ามลบ** — ทำเครื่องหมาย `♻ เลิกใช้ (แทนด้วย V-xxx)` เพื่อไม่ให้การ์ดเก่าอ้างถึงของที่หายไป
5. คีย์ i18n ของแต่ละรหัสตั้งชื่อล้อกัน (`validation.<slug>` / `error.<slug>`) และต้องมีทั้ง TH และ EN
6. ชนิด: `error` = กั้นไม่ให้ไปต่อ แสดงใต้ช่องหรือ banner · `notice` = แจ้งการปรับอัตโนมัติ ไปต่อได้
7. **ขอบเขตทะเบียนนี้:** เฉพาะข้อความชนิด error และ notice — **ป้ายสถานะและข้อความเชิงข้อมูล**
   (เช่น `ยังไม่ฟื้น`, `ไม่พบช่วงขาดทุนในช่วงเวลาที่เลือก`, เครื่องหมายปีไม่เต็ม, คำอธิบายศัพท์)
   เป็น copy ปกติใน i18n ไม่ต้องมีรหัส — คำอธิบายศัพท์การเงินมีทะเบียนของตัวเองที่
   [`metric-glossary.md`](metric-glossary.md) (PD-005)

## รหัสที่ใช้อยู่

| รหัส | ชนิด | ใช้เมื่อ | ข้อความ (TH) | i18n key | ใช้โดย | สถานะ |
| --- | --- | --- | --- | --- | --- | --- |
| V-001 | error | น้ำหนักรวมของทุกสินทรัพย์ไม่เท่ากับ 100% | `น้ำหนักรวมของพอร์ตต้องเท่ากับ 100% ตอนนี้รวมได้ {sum}%` | `validation.weightSum` | US-05, US-06 | ใช้งาน |
| V-002 | error | กดเริ่มทดสอบโดยยังไม่มีสินทรัพย์ | `เพิ่มสินทรัพย์อย่างน้อย 1 ตัวก่อนเริ่มทดสอบ` | `validation.assetRequired` | US-05 | ใช้งาน |
| V-003 | error | สัญลักษณ์ที่กรอกไม่มีข้อมูล | `ไม่พบข้อมูลของ {symbol} ตรวจสอบตัวสะกดอีกครั้ง` | `validation.symbolUnknown` | US-05, US-06, US-07 | ใช้งาน |
| V-004 | error | ปีเริ่มต้นมากกว่าปีสิ้นสุด (ปีเดียวกันใช้ได้) | `ปีเริ่มต้นต้องไม่มากกว่าปีสิ้นสุด` | `validation.rangeOrder` | US-05 | ใช้งาน |
| V-005 | error | ช่วงเวลาสิ้นสุดเกินเดือนที่มีข้อมูลจริง | `เลือกช่วงเวลาได้ถึง {lastMonth} ซึ่งเป็นเดือนล่าสุดที่มีข้อมูลครบ` | `validation.rangeFuture` | US-05 | ใช้งาน |
| V-006 | error | เงินตั้งต้นเป็น 0, ติดลบ, หรือไม่ใช่ตัวเลข | `เงินตั้งต้นต้องเป็นตัวเลขที่มากกว่า 0` | `validation.amountPositive` | US-05 | ใช้งาน |
| V-007 | error | น้ำหนักรายตัวอยู่นอก 0–100 หรือไม่ใช่ตัวเลข | `น้ำหนักแต่ละตัวต้องเป็นตัวเลขระหว่าง 0 ถึง 100` | `validation.weightRange` | US-05 | ใช้งาน |
| V-008 | error | ลิงก์อ่านโครงสร้างไม่ออกหรือเกินขอบเขตโครงสร้าง (ดู BR-URL-06) | `ลิงก์นี้มีค่าตั้งต้นไม่ครบหรือไม่ถูกต้อง กรุณาตั้งค่าพอร์ตใหม่` | `validation.linkInvalid` | US-06, US-11 | ใช้งาน |
| V-009 | error | ช่วงร่วมของข้อมูลมีไม่ถึงสองเดือนที่ปิดแล้ว | `ช่วงเวลาที่เลือกสั้นเกินไป ต้องมีข้อมูลอย่างน้อย 2 เดือน ลองขยายช่วงเวลา` | `validation.rangeTooShort` | US-02, US-07 | ใช้งาน |
| V-010 | error | กรอกสัญลักษณ์ที่มีอยู่แล้วในพอร์ตเดียวกัน | `สัญลักษณ์นี้มีอยู่ในพอร์ตแล้ว` | `validation.symbolDuplicate` | US-05 | ใช้งาน |
| N-001 | notice | ช่วงเวลาถูกย่ออัตโนมัติตามข้อมูลที่มีจริงของสินทรัพย์ | `ช่วงเวลาถูกปรับเป็น {start} ถึง {end} ตามข้อมูลที่มีของ {symbol}` | `notice.rangeClamped` | US-02, US-05, US-07 | ใช้งาน |
| E-001 | error | โหลดข้อมูลราคาไม่สำเร็จ (แหล่งข้อมูลไม่ตอบ/เครือข่ายขัดข้อง) | `โหลดข้อมูลราคาไม่สำเร็จ ลองใหม่อีกครั้ง` | `error.dataLoad` | US-03, US-05, US-07, US-11 | ใช้งาน |
| E-002 | error | สินทรัพย์ในพอร์ตไม่มีเดือนที่มีข้อมูลตรงกันเลย | `สินทรัพย์ในพอร์ตไม่มีช่วงเวลาที่มีข้อมูลตรงกัน ปรับช่วงเวลาหรือเปลี่ยนสินทรัพย์` | `error.noOverlap` | US-04, US-07 | ใช้งาน |
| N-002 | notice | ผลลัพธ์มีการแปลงค่าเงินเพราะสินทรัพย์คนละสกุลกับสกุลเงินฐาน | `ตัวเลขทั้งหมดคิดเป็น{currency}แล้ว โดยแปลงค่าเงินตามอัตราแลกเปลี่ยนของแต่ละเดือน` | `notice.converted` | US-13 | ใช้งาน |
| E-003 | error | ดึงอัตราแลกเปลี่ยนไม่สำเร็จ ทำให้แปลงค่าเงินไม่ได้ | `โหลดอัตราแลกเปลี่ยนไม่สำเร็จ จึงคำนวณพอร์ตที่มีหลายสกุลเงินไม่ได้ ลองใหม่อีกครั้ง` | `error.fxLoad` | US-12, US-13 | ใช้งาน |
| N-003 | notice | บางปีในช่วงที่เลือกยังไม่มีดัชนีเงินเฟ้อประกาศ จึงยังไม่ถูกปรับ | `ยังไม่มีข้อมูลเงินเฟ้อของ {years} ปีเหล่านี้จึงยังไม่ถูกปรับ` | `notice.inflationGap` | US-15 | ใช้งาน |

## รหัสที่คาดว่าจะต้องมี (ยังไม่ผูกการ์ด)

- เฟส 2: ข้อความฝั่ง cashflow (จำนวนเงินรายงวด, ความถี่) และ rebalancing bands
- กองทุนรวมไทย (ไม่พบกองทุน, รหัสกองทุนผิดรูป) — พักไว้จนกว่าจะมีคีย์ของ ก.ล.ต. (PD-012)
- เฟส 2: ข้อความฝั่งเงินเข้าออกและการปรับสมดุลแบบมีช่วง
