# US-28: ตารางความสัมพันธ์ระหว่างสินทรัพย์

**Epic แม่:** [`./epic.md`](./epic.md) · **Status:** 🟢 Ready (S17) · เส้นทาง: ⚪ → 🟡 → **🟢 Ready** → 🔨 In Dev → ✅ Done
**Priority:** P0 · **Session:** S18 · **Roadmap:** [Phase 4 · feature 4.1](../../../ROADMAP.md) · **Route:** `/backtest?<ค่าที่ตั้ง>` · **Data mode:** จริง

> **ในฐานะ** คนที่ถือของหลายตัวเพราะคิดว่ากระจายความเสี่ยงแล้ว **ฉันต้องการ** เห็นว่าตัวไหนขึ้นลงพร้อมกัน **เพื่อที่จะ** รู้ว่าที่ถืออยู่กระจายจริงหรือแค่ซื้อของคล้ายกันหลายชิ้น

**หลักที่ยึด:** P2 เข้าใจได้โดยไม่ต้องจบการเงิน · P1 เลขเชื่อถือได้

---

## บริบท & เงื่อนไขก่อนหน้า (Context & Preconditions)

- การกระจายความเสี่ยงได้ผลก็ต่อเมื่อของที่ถือ**ไม่ขึ้นลงพร้อมกัน** — ถือหุ้นสหรัฐห้ากองที่วิ่งตามกัน
  ไม่ต่างจากถือกองเดียวในแง่ความเสี่ยง แต่ผู้ใช้มองไม่เห็นเรื่องนี้จากหน้าจอวันนี้เลย
- ภาคผนวก A มีค่าอ้างอิงของ**สามคู่** ให้ตรวจ: VTI–VXUS `0.83` · VTI–BND `0.36` · VNQ–BND `0.60`
- ต้องใช้ชุดผลตอบแทน**รายสินทรัพย์** ซึ่งอยู่ในฟังก์ชันใหม่แยกตาม [PD-020](../../decision-log.md)

---

## กฎทางธุรกิจ (Business Rules)

| Rule ID | กฎ | เจ้าของค่า | Priority |
| --- | --- | --- | --- |
| BR-RSK-27 | **ความสัมพันธ์** ของสองชุด = ความแปรปรวนร่วม ÷ ผลคูณของส่วนเบี่ยงเบนมาตรฐานทั้งสอง คิดจากผลตอบแทนรายเดือน · **ไร้หน่วย** อยู่ในช่วง `−1` ถึง `+1` | ชั้นคำนวณ | P0 |
| BR-RSK-28 | ตารางครอบคลุม**ทุกสินทรัพย์ในพอร์ตนั้น บวกตัวเทียบ** เพื่อให้เห็นว่าของที่ถืออยู่วิ่งตามตลาดแค่ไหน | หน้าจอ | P0 |
| BR-RSK-29 | แสดงเป็น**ตารางสามเหลี่ยม** เพราะค่าของคู่ ก–ข เท่ากับ ข–ก เสมอ · ครึ่งบนที่ซ้ำกันไม่ต้องแสดง | หน้าจอ | P1 |
| BR-RSK-30 | เส้นทแยงมุม (ตัวเองกับตัวเอง) เท่ากับ `1.00` เสมอ — แสดงไว้เพื่อให้อ่านตารางออก ไม่ใช่ค่าที่มีความหมาย | หน้าจอ | P2 |
| BR-RSK-31 | สินทรัพย์ที่ผลตอบแทนไม่ขยับเลยตลอดช่วง → ส่วนเบี่ยงเบนมาตรฐานเป็นศูนย์ → คู่ที่มีมันอยู่ **ไม่มีค่า** ไม่ใช่ศูนย์ (BR-RSK-03) | ชั้นคำนวณ | P0 |
| BR-RSK-32 | ตารางแสดง**ทีละพอร์ต** พร้อมตัวเลือกพอร์ตเมื่อเทียบหลายพอร์ต (BR-RSK-06) · พอร์ตเดียวไม่มีตัวเลือก | หน้าจอ | P0 |
| BR-RSK-33 | ต้องมีคำอธิบายที่บอกว่า**เลขสูงแปลว่าอะไร**ในภาษาที่คนไม่ได้เรียนการเงินเข้าใจ ไม่ใช่แค่โชว์ตัวเลข | หน้าจอ | P0 |
| BR-RSK-34 | พอร์ตที่มีสินทรัพย์เดียว → ตารางมีแค่ตัวมันกับตัวเทียบ ซึ่งยังมีประโยชน์ ไม่ใช่ซ่อนส่วนนี้ทิ้ง | หน้าจอ | P1 |

---

## นิยามและค่าคาดหวัง (ตาม BR-RSK-02)

| เมทริก | หน่วยเวลา | ค่าคาดหวัง | ที่มาของค่า |
| --- | --- | --- | --- |
| ความสัมพันธ์ VTI–VXUS | ไร้หน่วย (จากรายเดือน) | `0.83` | **ต้นแบบ** (ภาคผนวก A) |
| ความสัมพันธ์ VTI–BND | ไร้หน่วย | `0.36` | **ต้นแบบ** |
| ความสัมพันธ์ VNQ–BND | ไร้หน่วย | `0.60` | **ต้นแบบ** |
| คู่ที่เหลือในตาราง | ไร้หน่วย | ดูชุดคำนวณมือ | **ชุดคำนวณมือ** |

**ชุดคำนวณมือ** — ค่าที่ได้เป็นเลขกลม ตรวจด้วยกระดาษได้ทันที

| ชุด ก | ชุด ข | ค่าคาดหวัง | พิสูจน์อะไร |
| --- | --- | --- | --- |
| `[1%, 2%, 3%, 4%]` | ชุดเดียวกัน | **`+1.00`** | ตัวเองกับตัวเองต้องได้หนึ่งพอดี (BR-RSK-30) |
| `[1%, 2%, 3%, 4%]` | `[−1%, −2%, −3%, −4%]` | **`−1.00`** | สวนทางกันสนิทต้องได้ลบหนึ่ง |
| `[1%, 2%, 3%, 4%]` | `[2%, 4%, 6%, 8%]` | **`+1.00`** | **ความสัมพันธ์ไม่สนขนาด สนแต่ทิศ** — ตัวที่เหวี่ยงแรงกว่าสองเท่าแต่ไปทางเดียวกันยังได้หนึ่ง |
| `[1, −1, 1, −1]` | `[1, 1, −1, −1]` | **`0.00`** | ไม่มีความสัมพันธ์กันเลยได้ศูนย์พอดี |
| ชุดใดก็ได้ | ชุดที่ไม่ขยับเลย `[0,0,0,0]` | **ไม่มีค่า** | ตัวหารเป็นศูนย์ต้องคืนไม่มีค่า ไม่ใช่ศูนย์ (BR-RSK-31) |

> **ชุด `+1.00` สามชุดแรกพิสูจน์อะไรไม่ได้:** พิสูจน์ไม่ได้ว่าตัวหารใช้ `n` หรือ `n−1`
> เพราะความสัมพันธ์เป็นอัตราส่วนที่ตัวหารตัดกันหมด · ข้อดีคือเรื่องนี้ไม่สำคัญสำหรับเมทริกนี้
> แต่**สำคัญมากสำหรับ [US-29](./US-29-decomposition.md)** ที่ใช้ความแปรปรวนตรง ๆ

---

## เกณฑ์ยอมรับ (Acceptance Criteria)

| AC | Given | When | Then |
| --- | --- | --- | --- |
| AC-RSK-17 | พอร์ตอ้างอิง `VTI 48 / VNQ 8 / VXUS 24 / BND 20` ช่วง 2012–2026 | ดูตารางความสัมพันธ์ | • VTI–VXUS `0.83` · VTI–BND `0.36` · VNQ–BND `0.60` ตรงต้นแบบภายใน ±0.1 |
| AC-RSK-18 | พอร์ตเดียวกัน | ดูโครงตาราง | • มีสี่สินทรัพย์บวกตัวเทียบ รวมห้าแถวห้าคอลัมน์<br>• เส้นทแยงมุมเป็น `1.00` ทุกช่อง<br>• ครึ่งบนที่ซ้ำกันไม่แสดง |
| AC-RSK-19 | เทียบสามพอร์ต | ดูส่วนนี้ | • มีตัวเลือกพอร์ต และแสดงทีละพอร์ต (BR-RSK-32) |
| AC-RSK-20 | พอร์ตเดียว | ดูส่วนนี้ | • ไม่มีตัวเลือกพอร์ตให้กดโดยไม่จำเป็น |
| AC-RSK-21 | ตารางแสดงอยู่ | อ่านคำอธิบายประกอบ | • บอกได้ว่าเลขใกล้ `1` แปลว่าขึ้นลงพร้อมกัน และแปลว่ากระจายความเสี่ยงได้น้อย |
| AC-RSK-22 | พอร์ตที่มีสินทรัพย์เดียว | ดูส่วนนี้ | • ตารางยังแสดง มีตัวมันกับตัวเทียบ (BR-RSK-34) |
| AC-RSK-23 | พอร์ตที่ตั้งเงินเข้าออกและเปิดปรับเงินเฟ้อ | ดูตาราง | • ค่าไม่เปลี่ยน (BR-RSK-08) |
| AC-RSK-24 | จอกว้าง 375 จุด | ดูตาราง | • เลื่อนแนวนอนในกรอบตัวเอง หน้าเว็บไม่เลื่อน |

---

## กฎการตรวจสอบ (Validation Rules)

| ช่อง | กฎ | ข้อความ |
| --- | --- | --- |
| — | การ์ดนี้ไม่มีช่องรับค่าใหม่ | — |

---

## Edge Cases (อย่างน้อย 3)

| # | สถานการณ์ | พฤติกรรมที่คาดหวัง |
| --- | --- | --- |
| EC-RSK-09 | พอร์ตที่มีสินทรัพย์เต็มสิบตัว | ตารางสิบเอ็ดแถว เลื่อนในกรอบตัวเองได้ ไม่ดันหน้าเว็บ |
| EC-RSK-10 | สินทรัพย์เดียวกันอยู่ในพอร์ตที่เลือกและเป็นตัวเทียบด้วย (เช่นถือ SPY แล้วเทียบกับ SPY) | คู่นั้นได้ `1.00` ซึ่งถูกต้อง และไม่ทำให้ตารางพัง |
| EC-RSK-11 | ช่วงข้อมูล 2 เดือน | ความสัมพันธ์คำนวณได้แต่ไม่มีความหมาย · การ์ดไม่ห้าม แต่คำอธิบายควรบอกจำนวนเดือนที่ใช้ |
| EC-RSK-12 | สินทรัพย์ที่ราคานิ่งสนิททั้งช่วง | คู่ที่มีมันอยู่แสดงเป็นขีดพร้อมเหตุผล ไม่ใช่ `0.00` ที่ทำให้เข้าใจผิดว่ากระจายได้ดี |

---

## การจัดการ Error (Error Handling)

| Error | เกิดเมื่อ | ผู้ใช้เห็นอะไร | ทางแก้ |
| --- | --- | --- | --- |
| ค่าคำนวณไม่ได้ | สินทรัพย์ตัวใดตัวหนึ่งไม่ขยับเลย | ขีดในช่องนั้นพร้อมเหตุผล ช่องอื่นยังแสดงปกติ | ไม่ต้องแก้ |

---

## หมายเหตุ UX

**หน้าจอ/ส่วน:** ส่วนใหม่ในหน้าผลลัพธ์ วางถัดจากส่วนหน้าต่างเลื่อน

```
ของที่ถืออยู่ขึ้นลงพร้อมกันแค่ไหน                    [พอร์ต: ผสม ▾]
ⓘ เลขใกล้ 1 แปลว่าสองตัวนี้ขึ้นลงไปทางเดียวกันเกือบตลอด
   ถือคู่กันจึงแทบไม่ช่วยกระจายความเสี่ยง · เลขใกล้ 0 แปลว่าไปคนละทาง
ⓘ ส่วนนี้คิดจากผลตอบแทนของพอร์ตล้วน ๆ ไม่รวมเงินเข้าออกและไม่หักเงินเฟ้อ

┌──────────┬───────┬───────┬───────┬───────┬───────┐
│          │  VTI  │  VNQ  │ VXUS  │  BND  │  SPY  │
├──────────┼───────┼───────┼───────┼───────┼───────┤
│ VTI      │  1.00 │       │       │       │       │
│ VNQ      │  0.72 │  1.00 │       │       │       │
│ VXUS     │  0.83 │  0.68 │  1.00 │       │       │  ← ค่าจากต้นแบบ
│ BND      │  0.36 │  0.60 │  0.31 │  1.00 │       │  ← ค่าจากต้นแบบ
│ SPY      │  0.99 │  0.71 │  0.82 │  0.34 │  1.00 │
└──────────┴───────┴───────┴───────┴───────┴───────┘
 ค่าที่ไม่ได้มาจากต้นแบบในภาพนี้เป็นตัวอย่างเพื่อให้เห็นรูปแบบ ไม่ใช่ค่าคาดหวัง

VTI กับ SPY ได้ 0.99 — เป็นตัวอย่างที่อ่านแล้วเข้าใจทันทีว่า "ถือสองตัวนี้เหมือนถือตัวเดียว"
ซึ่งเป็นคุณค่าหลักของส่วนนี้
```

**5 สถานะ UI:**

| สถานะ | เกิดเมื่อ | ผู้ใช้เห็นอะไร |
| --- | --- | --- |
| ว่าง (Empty) | ยังไม่เคยรัน | ไม่มีส่วนนี้ |
| กำลังโหลด (Loading) | กำลังคำนวณ | โครงร่างตารางเหมือนส่วนอื่น |
| ผิดพลาด (Error) | ไม่มีกรณีเฉพาะของการ์ดนี้ | ใช้พฤติกรรมเดิม |
| สำเร็จ (Success) | คำนวณเสร็จ | ตารางสามเหลี่ยมครบทุกคู่ |
| สำเร็จบางส่วน (Partial) | บางคู่คำนวณไม่ได้ | ช่องนั้นเป็นขีดพร้อมเหตุผล ช่องอื่นปกติ |

**การโต้ตอบสำคัญ:** เลือกพอร์ตจากตัวเลือก → ตารางเปลี่ยนทั้งชุด · เปิดคำอธิบายศัพท์ได้เหมือนที่อื่น

**การเข้าถึง (WCAG AA):** ตารางมีหัวแถวและหัวคอลัมน์ผูกกับเซลล์ อ่านทีละช่องแล้วรู้ว่าเป็นคู่ไหน ·
ค่าที่ไม่มีอ่านออกว่า "ไม่มีค่า" · ตัวเลือกพอร์ตเข้าถึงด้วยแป้นพิมพ์ได้

**Responsive:** ตารางเลื่อนแนวนอนในกรอบของตัวเองโดยคอลัมน์ชื่อสินทรัพย์ยังอ่านได้ · หน้าไม่เลื่อนแนวนอน

**ข้อจำกัดข้อมูลจำลอง (stub limitation):** ไม่มี

**คำเตือนการลงทุน:** ข้อความเดิมตาม BR-MVP-02

---

## แผนตรวจสอบ (Verification Plan)

- **ชุดทดสอบ:** สามคู่ที่ต้นแบบมีตรงภายใน ±0.1 · ชุดคำนวณมือห้าชุดตรงตามตาราง ·
  ตารางสมมาตร (ค่าของ ก–ข เท่ากับ ข–ก) · เส้นทแยงมุมเป็นหนึ่งทุกช่อง
- **บน route:** เดินพอร์ตอ้างอิงแล้วอ่านสามคู่ที่มีค่าอ้างอิง · เทียบสามพอร์ตแล้วสลับตัวเลือกพอร์ต ·
  พอร์ตสินทรัพย์เดียว · จอ 375 จุด · สองธีม
- **Guards:** lint · tsc · vitest · build

---

## Definition of Ready (DoR)

- [x] AC ทุกข้อทดสอบได้ตามที่เขียน
- [x] ข้อมูล/fixture ที่ต้องใช้ระบุครบ
- [x] แผนตรวจสอบเขียนแล้ว
- [x] Dependency เคลียร์ — ชุดผลตอบแทนรายสินทรัพย์มีอยู่แล้วที่ชั้นบน ([PD-020](../../decision-log.md))
- [x] ไม่มีคำถามค้างที่บล็อกการเริ่มงาน

## Definition of Done (DoD) + INVEST

**Definition of Done:**
- [ ] AC ผ่านครบบน route จริง
- [ ] **สามคู่ที่ต้นแบบมีให้เทียบ ตรงภายใน ±0.1**
- [ ] ค่าที่คำนวณไม่ได้แสดงเป็นขีดพร้อมเหตุผล ไม่ใช่ศูนย์
- [ ] คำอธิบายบอกได้ว่าเลขสูงแปลว่าอะไรในภาษาคนทั่วไป (BR-RSK-33)
- [ ] edge case ครบ
- [ ] R15 ถูกชั้น: อยู่ในฟังก์ชันบริสุทธิ์ที่รับรายการสินทรัพย์ ไม่แตะ `portfolioReturns`
- [ ] R17 หลักฐานครบ

**INVEST self-check:**
- [x] **I**ndependent · [x] **N**egotiable · [x] **V**aluable · [x] **E**stimable · [x] **S**mall · [x] **T**estable

---

## Implementation & Evidence (เติมตอน MODE B)

**ไฟล์ที่แตะ (ตามชั้น R15):**

| ชั้น | ไฟล์ | ทำอะไร |
| --- | --- | --- |
| | | |

**ผล guards:** lint · tsc · vitest · build
**เดิน route จริง:** URL ที่เดิน · ปุ่มที่กด · สิ่งที่เห็น · ภาพหน้าจอ
**Sweep:** —

---

## Traceability & Change Log

**Maps to:** [`./epic.md`](./epic.md) §1 ปัญหาข้อ 1 · Roadmap Phase 4 feature 4.1 · [PD-020](../../decision-log.md)

| Version | Date | Author | Changes |
| --- | --- | --- | --- |
| v1.0 | 2026-07-31 | PO (AI ช่วยร่าง) | story card แรก (S17) |
